# Development extras. Install on top of requirements.txt:
#     pip install -r requirements-dev.txt
-r requirements.txt

pytest>=7.4
pillow>=10.0        # only for scripts/make_test_scene.py
imageio-ffmpeg>=0.4 # alternative MP3 decoder if miniaudio is unavailable
